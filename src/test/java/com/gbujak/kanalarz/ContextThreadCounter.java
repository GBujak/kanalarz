package com.gbujak.kanalarz;

import java.util.HashMap;
import java.util.Map;
import java.util.UUID;

class ContextThreadCounter {

    private final Map<UUID, Integer> contextThreadCount = new HashMap<>();

    void increment(UUID contextId) {

    }
}
